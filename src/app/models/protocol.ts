export interface Protocol {
  USER_ID: number;
  MASTER_CLIENT_ID: number;
  MASTER_CLIENT_NAME: string;
  MASTER_PROTOCOL_ID: number;
  MASTER_PROTOCOL: string;
  MODULE_ID: number;
  USER_PROTOCOL_ROLE: string;
  isSelected?: boolean;
}
